<Window x:Class="RincoMTO.Tools.MtoCheck.UI.MtoCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MTO Check - Compare Rebar" 
        Width="900" Height="550"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6"
        Foreground="#1F2937"
        FontFamily="Segoe UI">
    <Window.Resources>
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Kiểm tra sai lệch thép giữa nhiều bản vẽ" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Sheet Selection Header + Buttons -->
        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Chọn Sheet MTO (tự tìm bản gốc bằng cách bỏ &quot;_MTO&quot;):" 
                       FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Chọn tất cả" Command="{Binding SelectAllCommand}" 
                    Style="{StaticResource SecondaryButton}" Margin="0,0,5,0"/>
            <Button Grid.Column="2" Content="Bỏ chọn" Command="{Binding DeselectAllCommand}" 
                    Style="{StaticResource SecondaryButton}"/>
        </Grid>

        <!-- Search Box -->
        <TextBox Grid.Row="2" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                 Height="28" Padding="5,3" Margin="0,0,0,5" BorderBrush="#D1D5DB"
                 ToolTip="Tìm kiếm theo số hoặc tên sheet"
                 Tag="🔍 Tìm kiếm sheet..."/>

        <!-- Sheet List with Checkboxes -->
        <ListBox Grid.Row="3" ItemsSource="{Binding AvailableSheets}" 
                 BorderBrush="#D1D5DB" BorderThickness="1" Background="White"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding DisplayName}" 
                              Margin="2" FontSize="12"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="2,1"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Results Table -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding Results}" AutoGenerateColumns="False" 
                  IsReadOnly="True" AlternatingRowBackground="#F9FAFB" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E5E7EB" BorderBrush="#D1D5DB"
                  Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sheet" Binding="{Binding SheetName}" Width="140"/>
                <DataGridTextColumn Header="Issue Type" Binding="{Binding IssueType}" Width="120"/>
                <DataGridTextColumn Header="Element ID" Binding="{Binding ElementId}" Width="80"/>
                <DataGridTextColumn Header="Family Name" Binding="{Binding FamilyName}" Width="120"/>
                <DataGridTextColumn Header="Parameter" Binding="{Binding ParameterName}" Width="100"/>
                <DataGridTextColumn Header="Source Value" Binding="{Binding SourceValue}" Width="90"/>
                <DataGridTextColumn Header="Target Value" Binding="{Binding TargetValue}" Width="90"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="5" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#4B5563" VerticalAlignment="Center" 
                       TextWrapping="Wrap"/>
            <Button Grid.Column="1" Content="RESET MÀU" Command="{Binding ResetColorsCommand}" 
                    Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Padding="15,8" FontWeight="SemiBold" />
            <Button Grid.Column="2" Content="UPDATE" Command="{Binding FixMissingCommand}" 
                    IsEnabled="{Binding HasMissingItems}" Margin="0,0,8,0"
                    Padding="15,8" FontWeight="SemiBold" Cursor="Hand" BorderThickness="0"
                    Foreground="White">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Padding="{TemplateBinding Padding}"
                                Background="#D97706">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#D1D5DB"/>
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Grid.Column="3" Content="RUN CHECK" Command="{Binding RunCheckCommand}" 
                    Style="{StaticResource PrimaryButton}"/>
        </Grid>
    </Grid>
</Window>
