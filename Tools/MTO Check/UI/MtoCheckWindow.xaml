<Window x:Class="RincoMTO.Tools.MtoCheck.UI.MtoCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MTO Check - Compare Rebar" 
        Width="600" Height="450"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6"
        Foreground="#1F2937"
        FontFamily="Segoe UI">
    <Window.Resources>
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Kiểm tra sai lệch thép giữa 2 bản vẽ" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- View Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Source View (Bản gốc):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding AvailableViews}"
                          SelectedItem="{Binding SelectedSourceView}"
                          DisplayMemberPath="Name" Height="30" Padding="5"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Target View (Bản sao - Hiện tại):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding TargetViewName}" IsReadOnly="True" Height="30" Padding="5,0" VerticalContentAlignment="Center" Background="#E5E7EB"/>
            </StackPanel>
        </Grid>

        <!-- Results Table -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Results}" AutoGenerateColumns="False" 
                  IsReadOnly="True" AlternatingRowBackground="#F9FAFB" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E5E7EB" BorderBrush="#D1D5DB">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Issue Type" Binding="{Binding IssueType}" Width="150"/>
                <DataGridTextColumn Header="Element ID" Binding="{Binding ElementId}" Width="100"/>
                <DataGridTextColumn Header="Family Name" Binding="{Binding FamilyName}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#4B5563" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="RUN CHECK" Command="{Binding RunCheckCommand}" Style="{StaticResource PrimaryButton}"/>
        </Grid>
    </Grid>
</Window>
