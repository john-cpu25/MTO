<Window x:Class="RincoMTO.Tools.MTOQuery.UI.MtoQueryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MTO Query" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="MTO Query Results (Current View)" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" AlternatingRowBackground="#FFF0F0F0"
                  GridLinesVisibility="All" BorderBrush="#FFCCCCCC" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Family Name" Binding="{Binding FamilyName}" Width="*"/>
                <DataGridTextColumn Header="Family Type" Binding="{Binding TypeName}" Width="*"/>
                <DataGridTextColumn Header="Tag / T̻n c̢y" Binding="{Binding TagText}" Width="2*"/>
                <DataGridTextColumn Header="Count" Binding="{Binding Quantity}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Close" Width="100" Height="30" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
