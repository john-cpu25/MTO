<Window x:Class="RincoMTO.Tools.MtoGroupBar.UI.MtoGroupBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MTO Group Bar" Height="450" Width="550"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lapped Rebar Groups" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch" Height="30" Margin="0,0,10,0" VerticalContentAlignment="Center" 
                     TextChanged="TxtSearch_TextChanged" ToolTip="Search by Group Name or Remarks..."/>
            <Button Grid.Column="1" Content="Random Colors (Override Graphics)" Height="30" Padding="10,0" Click="BtnRandomColors_Click"/>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="DataGridGroups" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" AlternatingRowBackground="#FFF0F0F0"
                  GridLinesVisibility="All" BorderBrush="#FFCCCCCC" BorderThickness="1"
                  SelectionMode="Single" SelectionChanged="DataGridGroups_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Group Name" Binding="{Binding GroupName}" Width="*"/>
                <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80"/>
                <DataGridTemplateColumn Header="Color" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding DisplayColor}" BorderBrush="Black" BorderThickness="1" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="5,0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasError}" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Close" Width="100" Height="30" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
